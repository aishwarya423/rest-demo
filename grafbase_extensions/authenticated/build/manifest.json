{"manifest":"v1","id":{"name":"authenticated","version":"1.0.3"},"type":{"Authorization":{}},"sdk_version":"0.22.1","minimum_gateway_version":"0.47.1","description":"Prevent access to elements in the query when the user is not authenticated","sdl":"\"\"\"\nThe @authenticated directive restricts access to the annotated\nresource, ensuring that only authenticated users can access it.\n\nWhen applied to a field, object, interface, enum, or scalar,\nthis directive will validate that a valid authentication token\nis present in the request before allowing access.\n\nExample usage:\n```graphql\ntype User {\n  id: ID!\n  # Only authenticated users can see email\n  email: String! @authenticated\n  name: String\n}\n\n# Or applied to an entire type\ntype PrivateData @authenticated {\n  secretInformation: String!\n}\n```\n\"\"\"\ndirective @authenticated on FIELD_DEFINITION | OBJECT | INTERFACE | ENUM | SCALAR\n","readme":"# Authenticated extension\n\nProvides an `@authenticated` directive, for subgraphs, which prevents access to elements in the query when the user is not authenticated.\n\n## Install\n\n```toml\n# grafbase.toml\n[extensions.authenticated]\nversion = \"1.0\"\n```\n\nRun the install command before starting the gateway\n\n```bash\ngrafbase extension install\n```\n\n## Usage\n\n```graphql\n# subgraph schema\nextend schema\n  @link(\n    url: \"https://grafbase.com/extensions/authenticated/1.0.0\"\n    import: [\"@authenticated\"]\n  )\n\ntype Query {\n  public: String!\n  mustBeAuthenticated: String! @authenticated\n}\n```\n","repository_url":"https://github.com/grafbase/extensions/tree/main/extensions/authenticated","license":"Apache-2.0","permissions":[]}